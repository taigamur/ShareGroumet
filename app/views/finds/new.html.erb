<% provide(:title, "search restaurants")%>

<div class="col-md-6 col-md-offset-3">
    <h3>飲食店を検索</h3>
    
    <p>キーワード, キーワード + エリアで検索してください</p>
    <p>例 ( 百香亭 , 牛角 つくば) </p>

    <%= form_with url: finds_path, method: :get, local: true do |f| %>
        <div class="form-group">
            <%= f.label :name,"キーワード" %>
            <%= f.text_field :name,class: 'form-control'%>
        </div>
        <%= f.submit "検索",  class: 'btn btn-primary' %>
    <% end %>
    
    <%= link_to "戻る" , static_pages_mypage_path , class: 'btn btn-default finds_new_btn'%>
    <%= link_to "直接入力", new_micropost_path , class: 'btn btn-default finds_new_btn'%>

</div>
